'use strict'

var mongoose = require('mongoose');
var MongoClient = require('mongodb').MongoClient;

// this will start the stratum server for the pool
function StratumPool(poolConfig) {

}


// pass in the mongodb document containing the pool config
// creates a PoolConfig object
function PoolConfig(data) {


}


module.exports = StratumPool;
module.exports = PoolConfig;
